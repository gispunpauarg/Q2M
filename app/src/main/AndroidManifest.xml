<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ar.edu.unpa.uarg.metricas">

    <!-- PERMISOS REQUERIDOS POR LA LIBRERÍA ~ COMIENZO -->

        <!-- Permisos requeridos para las métricas "getSignalStrength" y "getConnectionType" -->
        <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
        <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
        <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- Aunque la métrica "getConnectionType" sólo requiere de este permiso -->
        <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

        <!-- Permiso requerido para las métricas "getLatency", "getJitter" y "getPacketLoss" -->
        <uses-permission android:name="android.permission.INTERNET" />

        <!-- Permisos requeridos para poder manejar el archivo XML en el que se guardan las métricas -->
        <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
        <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- PERMISOS REQUERIDOS POR LA LIBRERÍA ~ FINAL -->
</manifest>
